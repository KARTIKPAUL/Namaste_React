1.What is Microservice ?
Ans: The idea is to split application into a set of smaller, interconnected services instead of building a single monolithic applications. Each services handles a specific job, likes handling user accounts or managaging payments.

2.What is monolithic architecture ?
Ans: When we used to build large projects where everythings was bundled together. Imagine building an entire applications where all the Code-APIs, usres interface, databse connections, authentication, even notification services resides in one massive project with single code branch.

3.What is the difference between Monolith and Microservice ?
Ans: Follow the question number 1 and 2.

4.Why do we need useEffect Hook ?
Ans: If we have to do something after the rendercycle complete w e can pass it inside the 'useEffect'.

5.What is optional chaining ?
Ans: data?.response?.ok this is basically known as optional chaining.

6.What is Shimmer UI ?
Ans: Shimmer UI is a technique that shows placeholder content while data is loading, reducing wait time and keeping users engaged.

7.What is the difference between JS expression and JS statement?
Ans: At a high level: expressions produce values, while statements performs actions.

An expresssion is any unit of code that cen be evaluated for a value.Thibk of it like a phrase in a sentence that stands for specific things.

A Statement is an instruction that tells the Javacsript engine to "do somwthing" if an expression is a phrase, a statement is a complete sentence.

8.What is Conditional Rendering, explain with a code example ?
Ans: Conditional rendering is a term used to describe the processs of displaying different UI elements or components based on certain conditions.

9.What is CORS? 
Ans: CORS(Cross Origin Resource Sharing) is a securiry mechanism built into for web browser. It acts as a gatekeeper that allows or restricts a website from requesting resources that live on a differenet domain than the website itself.

10.What is async and await?
Ans: Async and Await are special keywords in Javascripts designed to make asynchronous code look and behave more like synchronous code.

11.What is the use of `const json = await data.json();` in getRestaurants()?
Ans: When we use fetch(), it does not immediately give us the data, instead it returns a response object as soon as the headers arrive.

